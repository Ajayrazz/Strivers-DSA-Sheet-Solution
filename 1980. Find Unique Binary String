class Solution {
    public String findDifferentBinaryString(String[] nums) {
        int n = nums.length;
        String ans = "";
        for(int i = 0; i < n; i++){
            char ch = nums[i].charAt(i);
            ans += (ch == '1' ? "0" : "1");
        }
        return ans;
    }
}
