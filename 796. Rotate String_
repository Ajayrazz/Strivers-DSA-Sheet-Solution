class Solution {
    public boolean rotateString(String s, String goal) {
        //Base Case
        if(s.length() != goal.length()){
            return false;
        }

        String new_string = s + s;
        if(new_string.contains(goal)){
            return true;
        }
        return false;
    }
}
